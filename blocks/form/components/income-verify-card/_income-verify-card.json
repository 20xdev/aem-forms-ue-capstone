{
  "definitions": [
    {
      "title": "Income Verify Options",
      "id": "income-verify-card",
      "plugins": {
        "xwalk": {
          "page": {
            "resourceType": "core/fd/components/form/radiobutton/v1/radiobutton",
            "template": {
              "jcr:title": "Income Verify Options",
              "fieldType": "radio-group",
              "fd:viewType": "income-verify-card",
              "orientation": "horizontal",
              "enum": ["account-aggregator", "salary-account", "bank-statement"],
              "enumNames": ["Account Aggregator", "Login to Salary Account", "Upload Bank Statement"],
              "optionDescriptions": ["Instant & secure, processed via RBI-regulated partner.", "Quick & hassle-free, processed via NetBanking.", "Processed via uploading bank statement of the last 6 months."],
              "badgeOption": "account-aggregator",
              "badgeText": "Recommended"
            }
          }
        }
      }
    }
  ],
  "models": [
    {
      "id": "income-verify-card",
      "fields": [
        {
          "component": "tab",
          "label": "Basic",
          "name": "basic"
        },
        {
          "...": "../../models/form-common/_basic-input-fields.json#/fields"
        },
        {
          "component": "text",
          "name": "enum",
          "label": "Options (Data Value)",
          "valueType": "string",
          "multi": true,
          "required": true
        },
        {
          "component": "text",
          "name": "enumNames",
          "label": "Options (Display Name)",
          "valueType": "string",
          "multi": true
        },
        {
          "component": "text",
          "name": "optionDescriptions",
          "label": "Option Descriptions",
          "valueType": "string",
          "multi": true,
          "description": "One description per option, in the same order as Options (Data Value)"
        },
        {
          "component": "text",
          "name": "badgeOption",
          "label": "Badge Option (Enum Value)",
          "valueType": "string",
          "description": "Data value of the option that shows a badge strip. Leave empty for none."
        },
        {
          "component": "text",
          "name": "badgeText",
          "label": "Badge Text",
          "valueType": "string",
          "description": "Text shown in the badge strip (e.g. Recommended)"
        },
        {
          "...": "../../models/form-common/_help-container.json#/fields"
        }
      ]
    }
  ]
}
